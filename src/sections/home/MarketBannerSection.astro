---
import { Image } from '@astrojs/image/components';
import { Whatsapp } from '@Assets';
import { BaseText, BaseTextTypes, BaseLink } from '@BaseComponents';
import { SectionsLayout } from '@Layouts';
---

<section class="reason-background">
  <SectionsLayout class="reason-section">
    <div class="content">
      <BaseText
        class="reason-title"
        tag={BaseTextTypes.TAGS.H3}
      >
        Por que Investir Em Refrigeração E Climatização Para O Seu Negócio?
      </BaseText>

      <BaseText
        class="reason-paragraph"
        tag={BaseTextTypes.TAGS.P}
      >
        A manutenção preventiva e corretiva dos sistemas de Refrigeração e
        Climatização garante uma experiência de compra segura e agradável à seus
        clientes.<br />
        Isso é fundamental para aumentar suas vendas!
      </BaseText>

      <BaseLink
        class="reason-btn"
        href="https://wa.link/dmsi6d"
        variation="blue"
      >
        <Image
          alt="Whatsapp"
          class="whatsapp"
          src={Whatsapp}
          title=""
        />Saiba Mais
      </BaseLink>
    </div>
  </SectionsLayout>
</section>

<style>
  .reason-background {
    background-image: url('../../assets/images/marketbanner-image.webp');
    @apply bg-cover bg-center;
  }
  .reason-section {
    @apply flex
    h-screen
    min-h-[700px]
    flex-col
    items-end
    justify-center;
  }

  .content {
    @apply max-w-[570px];
  }

  .reason-title {
    @apply mb-12 text-4xl;
  }

  .reason-paragraph {
    @apply mb-12
    max-w-[490px];
  }

  .reason-btn {
    @apply min-w-[195px];
  }

  .whatsapp {
    @apply w-7;
  }
</style>
