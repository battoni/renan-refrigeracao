---
interface Props {
  class?: string;
  variation?: string;
}

const { class: className, variation } = Astro.props;

const dynamicClasses = [className, variation ? `is-${variation}` : ''];
---

<div class:list={['card', dynamicClasses]}>
  <slot />
</div>

<style>
  .card {
    @apply mb-6 
    flex
    min-h-fit
    max-w-[430px]
    flex-col
    rounded-[15px] 
    border-2 
    border-solid
    border-transparent
    bg-gray
    p-10
    transition-all
    ease-in-out
    hover:border-navyBlue;
    box-shadow: 6px 4px 4px 0px rgba(0, 0, 0, 0.25);
  }

  .is-service {
    @apply rounded-[0]
    border-0
    bg-transparent
    p-0
    hover:border-0;
    box-shadow: none;
  }
</style>
